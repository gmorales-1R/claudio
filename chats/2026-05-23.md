# Session — 2026-05-23

## What was built

- Initialized `claudio` repo as a personal AI workbench
- Debugged GitHub PAT push permissions (`public_repo` scope required)
- Created: `claude.md`, `soul.md`, directory stubs for `agents/`, `knowledge/`, `tasks/`
- Redesigned memory system: index (`memory.md`) + cache (`current.md`) + `archive/`
- Created **Sandman** — first agent, handles memory org and categorization
- Added `web/` dir and `index.html` for Vercel deployment
- Connected repo to Vercel, resolved 404 by placing `index.html` at root
- Added `.gitignore` covering secrets, env files, and OS noise
- Established security rules and content guidelines in `claude.md`

## Decisions made

- Repo is public — no credentials, PII, or sensitive data ever committed
- `personal/` dir is gitignored — moot in cloud context, noted for future
- Chats are recorded and redacted before committing
- Files should stay simple and generic, grow from real usage

## Open threads

- Vercel root directory setting not found in mobile UI — using repo root as workaround
- Dashboard UI not yet built
- Sandman not yet fully exercised
